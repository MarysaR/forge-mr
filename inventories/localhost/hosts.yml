---
# Inventaire ForgeMR v0.0.1 : une seule cible, la machine locale (cf. ADR-0001, ADR-0002).
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3
